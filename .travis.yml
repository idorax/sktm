language: python
python:
    - "2.7"
env:
    - TOX_ENV=py27,flake8,pylint
install:
    - pip install -U pip
    - pip install -r requirements.txt
    - pip install -r test-requirements.txt
    - pip install tox
script:
    - tox -e $TOX_ENV
